const NoPosts = () => {
    return (
        <div className="h-full w-full flex items-center justify-center">
            <p className="text-slate-600 text-sm">This user hasn't posted anything yet</p>
        </div>
    );
};

export default NoPosts;
